name: step_3_mount

services:
  app:
    image: mount-example:latest
    container_name: mount-example
    build: .
    ports:
      - "3001:3000"
    volumes:
      - my-volume:/app/data

volumes:
  my-volume:
