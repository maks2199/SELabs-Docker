name: chaos-stage3

services:
  app:
    image: mount-example:latest
    container_name: mount-example
    build: ../../steps/step_3_mount/description/mount
    ports:
      - "3001:3000"
    volumes:
      - ./data:/wrong/path
